import React from 'react';
import {Image} from 'react-native';

const KeyFormIcon = () => {
  return (
    <Image
      style={{width: 32, height: 32}}
      source={require('./../../assets/icons/form/key.png')}
    />
  );
};

export default KeyFormIcon;
